# TASK-116B template only. Never place real values in a committed file.
# Resolve an approved official image tag to its immutable registry digest first.
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw@sha256:REPLACE_WITH_64_HEX_DIGEST

# Supply the gateway token at runtime from a secret manager or protected shell session.
# This no-egress scaffold deliberately receives no model/API key.
OPENCLAW_POC_GATEWAY_TOKEN=REPLACE_WITH_AT_LEAST_32_RANDOM_CHARACTERS
